/** Buddy art vs circle — keep in sync with BuddiesPage / BuddyInfoPage hero. */
export const BUDDY_IN_CIRCLE_W_PCT = 45;
export const BUDDY_IN_CIRCLE_H_PCT = 46;

/** ~one column width from the buddies grid (40rem / 48rem strip, two columns). */
export const BUDDY_PROFILE_CIRCLE_MAX = "max-w-[19rem] sm:max-w-[22.5rem]";

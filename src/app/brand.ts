/** Shared light orange ombre + layout — use with `tb-shell` / `tb-shell--scroll` from global CSS */
export const PAGE_SHELL = "tb-shell";

export const PAGE_SHELL_SCROLL = "tb-shell tb-shell--scroll";

/** Coral panel default text + headings — same neutral gray as field values */
export const INFO_PANEL_TEXT = "#2d2d2d";

/** Typed values in coral info fields — neutral gray */
export const INFO_PANEL_INPUT_TEXT = "#2d2d2d";

/** Intro / subtitle under page titles */
export const PAGE_INTRO_BLURB_TEXT = "#5a5a5a";

export const INTRO_BUDDY_IMG_CLASS = "tb-intro-buddy-img";

export const WELCOME_MASCOT_INITIAL_SCALE = 380 / 240;

export const HOME_BUDDY_IMG_CLASS = "tb-home-buddy-img";

export const HOME_HERO_STACK_CLASS = "tb-hero-stack";

export const HOME_HERO_HEADLINE_CLASS = "share-tech-regular tb-hero-headline";

/** motion `layoutId` for the orange mascot across start / welcome / home */
export const MASCOT_SHARED_LAYOUT_ID = "tastebuddy-main-mascot";
